{
  "DatabasePath": "data/openfork.db",
  "DefaultProviderKey": "openai",
  "DefaultModel": "gpt-4o",
  "OpenAiCompatible": {
    "openai": {
      "ApiUrl": "https://api.openai.com/v1",
      "ApiKeyEnv": "OPENAI_API_KEY",
      "AvailableModels": [
        {
          "Name": "gpt-4o",
          "DisplayName": "GPT-4o",
          "MaxTokens": 128000,
          "MaxOutputTokens": 16384,
          "Capabilities": {
            "Tools": true,
            "Images": true,
            "ParallelToolCalls": true
          }
        }
      ]
    }
  },
  "Agents": [
    {
      "Name": "coder",
      "SystemPrompt": "You are a coding assistant with full access to the file system and terminal.\n\nAvailable tools:\n- read_file: Read contents of a file\n- write_file: Write content to a file (creates if doesn't exist)\n- list_directory: List files and folders in a directory\n- create_directory: Create a new directory\n- run_command: Execute shell commands (git, npm, dotnet, etc.)\n- search_project: Semantic search across the project codebase\n- todo_write: Create/update task list to track progress on complex tasks\n- todo_read: Read current task list status\n\nFor complex tasks (3+ steps), use todo_write first to create a task list. Update todo status as you complete tasks. When asked to find code or understand the codebase, use search_project first. When asked to create files, use write_file directly. When asked to run commands, use run_command. Always use tools to perform operations - never just describe what you would do.\n\nBe concise and direct. Execute tasks immediately.",
      "ProviderKey": "openai",
      "Model": "gpt-4o",
      "MaxIterations": 30
    },
    {
      "Name": "planner",
      "SystemPrompt": "You are a planning agent. Analyze the request and produce a concise implementation plan with key steps and potential risks. You have access to file system tools to examine the codebase.",
      "ProviderKey": "openai",
      "Model": "gpt-4o"
    },
    {
      "Name": "executor",
      "SystemPrompt": "You are an execution agent. You implement tasks using the available file system tools. Read existing files to understand context, then create or modify files as needed. Be direct and execute immediately.",
      "ProviderKey": "openai",
      "Model": "gpt-4o",
      "MaxIterations": 0
    }
  ],
  "Pipelines": [
    {
      "Name": "plan_execute",
      "Description": "Planner then executor",
      "Steps": [
        {
          "Agent": "planner",
          "HandoffMode": "full"
        },
        {
          "Agent": "executor",
          "HandoffMode": "last"
        }
      ]
    }
  ],
  "Search": {
    "QdrantHost": "localhost",
    "QdrantPort": 6334,
    "OllamaUrl": "http://localhost:11434",
    "EmbeddingModel": "nomic-embed-text",
    "EmbeddingDimension": 768,
    "EnableSemanticSearch": true
  }
}